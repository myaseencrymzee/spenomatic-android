<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SpenoMatic" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="circularProgressStyle" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/theme_blue</item>
    </style>

    <style name="Theme.SpenoMatic" parent="Base.Theme.SpenoMatic" />
</resources>